// JavaScript

// 1️⃣ Разница между var, let, const?

// 👉 var — устарел, не имеет блочной области видимости.
// 👉 let — блочная область, можно менять значение.
// 👉 const — блочная область, нельзя менять саму переменную.

var a = 1; // Глобальная или функциональная область видимости
let b = 2; // Блочная область видимости
const c = 3; // Блочная область, неизменяемая переменная

// - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

// 2️⃣ Что такое замыкание (Closure)?
// 👉 Функция, которая "запоминает" переменные из своей внешней области.

function counter() {
  let count = 0;
  return () => count++;
}
const increment = counter();
console.log(increment()); // 0
console.log(increment()); // 1

// - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

// 3️⃣ Разница между == и ===?
// 👉 == сравнивает значения с приведением типов.
// 👉 === сравнивает без приведения типов.

console.log(5 == "5"); // true  (приведение типов)
console.log(5 === "5"); // false (разные типы)

// - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

// 4️⃣ Как работает this?
// 👉 this ссылается на объект, в контексте которого вызвана функция.
// 👉 В стрелочных функциях this берётся из внешней области.

const hero = {
  name: "Супермен",
  run() {
    console.log(`${this.name} бежит!`);
  },
};
hero.run(); // "Супермен бежит!"

// - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

// 5️⃣ Что такое event loop?
// 👉 Механизм, который позволяет JavaScript выполнять асинхронные задачи.
// 👉 Сначала выполняется синхронный код, потом задачи из callback queue.

console.log("Start");
setTimeout(() => console.log("Timeout"), 0);
console.log("End");
// Выведет: Start → End → Timeout

// - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

// 6️⃣ Разница между setTimeout и setInterval?
// 👉 setTimeout(fn, time) — выполняет код один раз после задержки.
// 👉 setInterval(fn, time) — выполняет код постоянно через заданный интервал

setTimeout(() => console.log("Раз"), 1000);
setInterval(() => console.log("Бесконечно"), 2000);

// - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

// 7️⃣ Как работает async/await?
// 👉 async делает функцию асинхронной, await ожидает выполнение Promise.

async function fetchData() {
  let res = await fetch("https://api.example.com");
  let data = await res.json();
  console.log(data);
}

// - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

// 8️⃣ Методы массива: .map(), .filter(), .reduce()
// 👉 .map(fn) — создаёт новый массив, применяя функцию к каждому элементу.
// 👉 .filter(fn) — оставляет только элементы, удовлетворяющие условию.
// 👉 .reduce(fn, acc) — сворачивает массив в одно значение.

const arr = [1, 2, 3, 4];
console.log(arr.map((x) => x * 2)); // [2, 4, 6, 8]
console.log(arr.filter((x) => x % 2 === 0)); // [2, 4]
console.log(arr.reduce((sum, x) => sum + x, 0)); // 10

// - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

// 9️⃣ Разница между null и undefined?
// 👉 null — значение отсутствует осознанно.
// 👉 undefined — переменная существует, но не имеет значения.

let a; // undefined
// let b = null; // null

// - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

// 🔟 Что такое localStorage, sessionStorage, cookies?
// 👉 localStorage — хранит данные навсегда, пока не удалишь.
// 👉 sessionStorage — данные удаляются при закрытии вкладки.
// 👉 cookies — передаются на сервер с каждым запросом.

// - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -


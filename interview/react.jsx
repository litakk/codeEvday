// 1️⃣ Что такое React?
// 👉 React — это библиотека JavaScript для создания пользовательских интерфейсов (UI).

// - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

// 2️⃣ Что такое JSX?
// 👉 JSX (JavaScript XML) — это синтаксис, который позволяет писать HTML внутри JavaScript.

// - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

const element = <h1>Hello, React!</h1>;
// 3️⃣ Что такое компоненты в React?
// 👉 Компоненты — это переиспользуемые части UI, которые могут быть функциональными или классовыми.

function Welcome() {
  return <h1>Привет!</h1>;
}

// - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

// 4️⃣ Разница между функциональными и классовыми компонентами?
// 👉 Функциональные — простые, используют useState, useEffect.
// 👉 Классовые — сложнее, используют this.state, this.setState().

// - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

// 5️⃣ Что такое state?
// 👉 state — это объект, который хранит данные внутри компонента и может изменяться.

import { useState } from "react";
function Counter() {
  const [count, setCount] = useState(0);
  return <button onClick={() => setCount(count + 1)}>Счет: {count}</button>;
}

// - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

// 6️⃣ Что такое props?
// 👉 props (свойства) передаются в компонент извне и неизменяемы внутри компонента.

function Welcome({ name }) {
  return <h1>Привет, {name}!</h1>;
}
<Welcome name="Иван" />;

// - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

// 7️⃣ Что такое useState и как он работает?
// 👉 useState — это хук для управления состоянием.
// 👉 Возвращает массив: текущее состояние и функцию для его обновления.

const [count, setCount] = useState(0);
setCount(count + 1);

// - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

// 8️⃣ Что такое useEffect и для чего он нужен?
// 👉 useEffect выполняет код при отрисовке и обновлении компонента.

useEffect(() => {
  console.log("Компонент обновился!");
}, [count]); // Выполняется при изменении count

// - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

// 9️⃣ Какой порядок рендеринга компонентов?
// 👉 1. Рендер (React строит виртуальный DOM).
// 👉 2. Сравнение (Reconciliation) (React проверяет, что изменилось).
// 👉 3. Обновление реального DOM (React обновляет только изменённые части).

// - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

// 🔟 Что такое виртуальный DOM?
// 👉 Виртуальный DOM — это копия реального DOM, которую React использует для эффективных обновлений.

// - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

// 1️⃣1️⃣ Что такое key в списках?
// 👉 key — это уникальный идентификатор, помогающий React отслеживать элементы списка.

const list = items.map(item => <li key={item.id}>{item.text}</li>);

// - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

// 1️⃣2️⃣ Как работает поднятие состояния (lifting state up)?
// 👉 Когда состояние нужно разделить между компонентами, его поднимают в родительский компонент.

function Parent() {
  const [value, setValue] = useState("");
  return <Child value={value} setValue={setValue} />;
}

// - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

// 1️⃣3️⃣ Как сделать обработку событий в React?
// 👉 Используем onClick, onChange, onSubmit и т. д.

<button onClick={() => alert("Нажато!")}>Нажми</button>

// - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

// 1️⃣4️⃣ Как создать контролируемый компонент?
// 👉 У контролируемого компонента value берётся из state, а изменения отслеживаются через onChange.

function Input() {
  const [text, setText] = useState("");
  return <input value={text} onChange={(e) => setText(e.target.value)} />;
}

// - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

// 1️⃣5️⃣ Как работать с формами в React?
// 👉 Используем useState и onSubmit.

function Form() {
  const [name, setName] = useState("");
  const handleSubmit = (e) => {
    e.preventDefault();
    console.log(name);
  };
  return (
    <form onSubmit={handleSubmit}>
      <input value={name} onChange={(e) => setName(e.target.value)} />
      <button type="submit">Отправить</button>
    </form>
  );
}

// - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

// 1️⃣6️⃣ Что такое React.Fragment (<> </>)?
// 👉 Это невидимый контейнер, который не добавляет лишний div в DOM.

<>
  <h1>Заголовок</h1>
  <p>Текст</p>
</>

// - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

// 1️⃣7️⃣ Что такое HOC (Higher Order Component)?
// 👉 HOC — это функция, которая принимает компонент и возвращает новый компонент с дополнительными пропсами.

const withLogging = (Component) => (props) => {
  console.log("Компонент отрендерен!");
  return <Component {...props} />;
};

// - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

// 1️⃣8️⃣ Что такое React.memo()?
// 👉 React.memo() оптимизирует ререндеринг, запоминая (мемоизируя) результат.

const MemoizedComponent = React.memo(MyComponent);

// - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

// 1️⃣9️⃣ Как создать контекст (Context API)?
// 👉 Context позволяет передавать данные без пропсов.


const ThemeContext = React.createContext("light");
function App() {
  return (
    <ThemeContext.Provider value="dark">
      <Child />
    </ThemeContext.Provider>
  );
}

// - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

// 2️⃣0️⃣ Что такое useRef и для чего он нужен?
// 👉 useRef создаёт ссылку на DOM-элемент или сохраняет данные между рендерами.

const inputRef = useRef();
<input ref={inputRef} />;

// - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
